// TypeScript FUNCTION-mode driver template.
// Judge0 language id 74 transpiles before running. {{SOLUTION}} = user's code.

{{SOLUTION}}

import * as fs from "fs";
const _data: string[] = fs.readFileSync(0, "utf8").split("\n");
// ---- problem-specific parsing ----
// const arg0 = JSON.parse(_data[0]);
// const result = solutionFunction(arg0);
// console.log(JSON.stringify(result).replace(/,/g, ", "));
