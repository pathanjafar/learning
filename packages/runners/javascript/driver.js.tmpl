// JavaScript FUNCTION-mode driver template.
// {{SOLUTION}} is replaced with the user's code (defining the required function).

{{SOLUTION}}

const _data = require("fs").readFileSync(0, "utf8").split("\n");
// ---- problem-specific parsing ----
// const arg0 = JSON.parse(_data[0]);
// const arg1 = parseInt(_data[1], 10);
// const result = solutionFunction(arg0, arg1);
// ---- canonical serialization (match Python: "[0, 1]") ----
// console.log(JSON.stringify(result).replace(/,/g, ", "));
