// Java FUNCTION-mode driver template.
// {{SOLUTION}} is replaced with the user's code, expected to define `class Solution`.
// The offline verifier's Java entrypoint must be a public class named Main.

import java.util.*;
import java.util.stream.*;

{{SOLUTION}}

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        // ---- problem-specific parsing goes here ----
        // ---- call new Solution().method(...) ----
        // ---- print result in the SAME textual form as the Python driver ----
    }
}
